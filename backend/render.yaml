services:
  - type: web
    name: questionaura-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT

databases:
  - name: questionaura-db
    databaseName: questionaura
    user: questionaura
